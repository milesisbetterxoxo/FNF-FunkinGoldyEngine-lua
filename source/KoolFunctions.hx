package;

import FunkinLua;
import PlayState;
import MusicBeatState;
import LoadingState;
import llua.Lua;
import llua.LuaL;
import llua.State;
import llua.Convert;
class KoolFunctions
{
    public var debugMode:Bool = false;
    /*public function loadLuaScript(folder:String, fileName:String)
    {
        var file:String = folder + '/' fileName + '.lua';
        luaArray.push(new FunkinLua(folder + '/' + fileName + '.lua'));
		filesPushed.push(file);
    }*/
    public function turnDebugMode():Void
    {
       debugMode = true; // haha funny thing
    }
    /*public function moveWindow(x:Int, y:Int)
	{
		Lib.application.window.move(x, y);
	}*/

 
			
}
// very easy coded thing. for like uhhh it creatin kool  functions
// REALLY COOL !!!111`11!~!!
// idk why does it bug as fuck